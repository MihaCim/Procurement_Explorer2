import styled from 'styled-components';

const PageContainer = styled.div`
  display: flex;
  padding: 8px 24px var(--gap-form-top, 40px) 24px;
  gap: 8px;
  flex-direction: column;
  align-items: flex-start;
  margin-top: 8px;
  align-self: stretch;
  width: 100%;
`;

export default PageContainer;
